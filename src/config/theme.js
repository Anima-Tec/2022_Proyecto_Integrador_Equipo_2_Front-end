import { defaultTheme } from '@xstyled/styled-components';

const theme = {
  ...defaultTheme,
  colors: {
    'icon-black': '#292D32',
    blue: '#014F86',
    white: '#FFFFFF',
    warning: '#D47171'
  }
};

export { theme };
