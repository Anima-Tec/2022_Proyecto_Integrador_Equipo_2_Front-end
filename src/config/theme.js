import { defaultTheme } from '@xstyled/styled-components';

const theme = {
  ...defaultTheme,
  colors: {
    blue: '#014F86',
    white: '#FFFFFF',
    warning: '#D47171'
  }
};

export default theme;
